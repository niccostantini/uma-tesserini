# UMA Festival - Template Configurazione Ambiente
# ===============================================
# Copia questo file in '.env' e modifica i valori come necessario

# Porta server (default: 3000 per sviluppo, 5173 per produzione)
PORT=3000

# Percorso database SQLite (viene creato automaticamente)
DB_PATH=uma.db

# Chiave HMAC per firma QR (OBBLIGATORIA)
# Genera una chiave con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# NON condividere mai questa chiave!
HMAC_SECRET_HEX=your_64_character_hex_string_here

# Ambiente di esecuzione
NODE_ENV=development

# Carica dati demo al primo avvio (0=no, 1=si)
LOAD_DEMO=0

# Livello log (error, warn, info, debug)
LOG_LEVEL=info

# ====================================================
# SETUP ISTRUZIONI:
# 
# 1. Copia questo file: cp .env.example .env
# 2. Genera chiave HMAC: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# 3. Incolla la chiave generata in HMAC_SECRET_HEX nel file .env
# 4. Installa dipendenze: npm install
# 5. Avvia server: npm start
# 6. Accedi a: http://localhost:3000
# 
# Il database uma.db verrà creato automaticamente al primo avvio
# Per dati demo: imposta LOAD_DEMO=1 prima del primo avvio
# ====================================================
